registry {
  type = "eureka"

  eureka {
    serviceUrl = "http://localhost:8761/eureka"
    #配置注册在eureka上的服务名
    application = "seata-server"
    weight = "1"
  }
}

config {
  type = "file"

  file {
    name = "file.conf"
  }
}
